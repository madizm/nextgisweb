# NextGIS Web Configuration
# Copy this file to config.local.cfg and customize

[nextgisweb]
# Database connection (auto-configured via environment variable)
# database.url = postgresql://nextgisweb:password@db:5432/nextgisweb

# Application secret key (required for sessions)
# secret = your_secret_key_change_in_production

# Cache directory
# cache.dir = /var/nextgisweb/cache

# Data directory  
# data.dir = /var/nextgisweb/data

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# log.level = INFO

# Optional: Sentry error tracking
# sentry.dsn = 

# Optional: Custom font for map rendering
# font = /path/to/font.ttf

# Server settings
# pyramid.reload_templates = false
# pyramid.debug_authorization = false
# pyramid.debug_notfound = false
# pyramid.debug_routematch = false
# pyramid.default_locale_name = en
